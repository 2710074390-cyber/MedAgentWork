# Agent 1 → Agent 2 调用指令模板 v2.1

> 用户将下方内容粘贴到 Agent 2 (MedGen) 对话框即可开始出题。
> 替换 `{...}` 占位符后使用。

---

```
═══════════════════════════════════════════════════════
📤 请将以下指令粘贴到 Agent 2 (MedGen) 的对话框中：
═══════════════════════════════════════════════════════

【任务】{科目名称}题库生成（{batch号}）

【教材重点与难点】（来源：{重点与难点文档名}）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
（粘贴各章节重点与难点内容）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【RAG检索验证】（教材关键片段，出题时以这些页码的教材原文为事实依据）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
（粘贴RAG检索返回的教材页码和关键内容）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【辅助素材】
- {素材路径1}
- {素材路径2}

【命题双向细目表】（HC-7）
┌──────────────┬──────────┬──────────┬──────────┬──────────┬──────────┐
│ 知识模块     │ 记忆30%  │ 理解40%  │ 应用25%  │ 分析5%   │ 题量     │
├──────────────┼──────────┼──────────┼──────────┼──────────┼──────────┤
│ {模块名}     │ {简述}   │ {简述}   │ {简述}   │ {简述}   │ ~{题量}  │
└──────────────┴──────────┴──────────┴──────────┴──────────┴──────────┘

题型分配目标：
  A1型（单选）：40%       — 基础概念/定义/病因
  A2型（病例单选）：20%    — 临床病例分析
  A3/A4型（病例串）：15%   — 复合病例分析
  B1型（共用选项）：10%    — 对比鉴别
  X型（多选题）：15%       — 综合知识

【特殊要求】
1. **目标考试**：{期末考试/考研西综/执业医师}
2. **每题必须标注溯源页码**：格式为「教材P###」
3. **NBME选项设计硬约束（HC-7 Gen）**：
   - 禁止绝对词（"总是""绝不"）
   - 选项长度控制在≤1.5倍（最长/最短），预平衡不截断
   - 保持语法一致性
   - 数值型选项按升序排列
   - 题干关键词不只在正确选项出现（词重复防护）
   - 正确选项术语共享计数不显著高于干扰项（收敛防护）
   - B1型共用选项避免笼统描述
4. **⛔ 绝后患铁律**：选项长度平衡用语义扩充短选项，绝不暴力截断长选项
5. **产出门禁**：生成后运行 `python validate_options.py --batch {batch号}` 自检
   - R2>2.0 或 R3/R4/R7/R8 fail>0 时必须修正后再交付
6. **防过度加长**：自然短术语（药名/受体/疾病名3-7字）豁免长度下限

【输出规范】
- 输出路径：中间产物/{batch号}/
- 文件格式：纯 JSON 数组（禁止 YAML 前置 `---` 元数据）
- 文件命名：ALL_questions_{batch号}_{系统名}.json
- 每题字段：id, type, module, module_name, topic, bloom, stem, options[{label,text}], answer, explanation, source_page, option_polarities

═══════════════════════════════════════════════════
```
